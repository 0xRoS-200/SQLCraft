fastapi==0.111.0
uvicorn==0.30.0
pydantic==2.7.0
requests==2.31.0

# PyTorch + HuggingFace (for local model inference)
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0

# OpenCV (Meta requirement — available but minimal usage in baseline)
opencv-python-headless>=4.9.0

# sqlite3 is built into Python standard library — no install needed
